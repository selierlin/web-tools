# Web Tools 页面设计规范

首页 `index.html`、`文本加工.html` 和 `JSON 格式化.html` 是本规范的样板。后续 AI 改造其他工具前，先读本文、`styles/workbench.css` 和对应样板页；项目行为指令优先读 AGENTS.md，不存在时读 CLAUDE.md。

## 设计方向

开发者工作台：深绿品牌栏、暖白画布、白色编辑面板、细边框。信息密度适中，输入和结果优先进入首屏。标题左对齐，避免宣传式大标题、玻璃背景、渐变按钮、粒子、光晕和持续动画。中文界面用系统无衬线字体，代码和行号用等宽字体。

首页负责导航、搜索、收藏、最近工具与 iframe 切换；子页面负责具体工具操作。子页面必须能够独立打开，不依赖父页面注入样式。尚未迁移的工具保持原样，逐页迁移。

## 文件与复用边界

- `styles/workbench.css`：共享颜色、字体、圆角、焦点和减少动态效果设置；是设计 token 的唯一来源。
- `styles/shell.css`：只由首页引用，管理品牌栏、工具导航、下拉和移动端菜单。
- `styles/text-process.css`：文本加工页布局与组件样板。
- `styles/json-format.css`：JSON 格式化页布局与组件样板；保留树形输出与原有脚本所需的状态颜色兼容 token。
- 新工具引用 `workbench.css`，使用自己的页面样式文件。优先复用 token，不直接引用文本加工样式以免误继承隐藏按钮等业务状态规则。
- 用新样式替换页面原有样式，避免在旧 CSS 尾部不断增加覆盖。共享文件只放真正通用的规则，不把某个工具的布局写成全局规则。

## 颜色与排版

| 用途 | Token / 值 |
| --- | --- |
| 页面画布 | `--wb-bg: #f5f6f2` |
| 面板 | `--wb-surface: #ffffff` |
| 正文 | `--wb-ink: #202e2a` |
| 次要文字 | `--wb-muted: #66736e` |
| 边框 | `--wb-line: #d9e0da` |
| 主操作、焦点 | `--wb-accent: #17654e` |
| 主操作悬停 | `--wb-accent-hover: #104d3b` |
| 选中背景 | `--wb-tint: #eaf3ed` |
| 警示、预览、收藏 | `--wb-warning: #98631b` |
| 首页品牌栏 | `#173e31`，白色文字 |

正文默认 16px；常用控件、编辑器和标签 14px；辅助统计 12px；提示 13px。页面标题桌面 26px、移动端 24px。正文行高 1.5，编辑器与行号同时为 `14px / 1.75`，两者垂直 padding 必须一致。不要仅缩小字体来塞入更多内容。

## 页面结构

首页品牌栏高 52px，只保留标识、名称和搜索，去掉英文副标题；桌面工具导航另占 48px，窄屏导航 50px。移动菜单和遮罩从 52px 处展开。

1. 页面标题：工具名称与短用途说明同行，可自然换行。桌面页边距 `20px 28px`，最大内容宽度 1680px，居中；不再保留占高的类别标签和装饰性右侧文案。
2. 工作面板：白底细边框，以「左侧处理面板 + 右侧双栏编辑」形成清晰分区，避免在超宽工具栏上散放少量控件。
3. 左侧处理面板：桌面宽 236px，浅灰绿底；集中放置选项和简短格式示例。示例仅用于说明，不写入输入框、不充当真实结果。输入后仍由原脚本决定显示哪些转换按钮。
4. 右侧编辑区：输入白底，结果浅绿底，通过编号、标签与色调区分；中间保留回传。桌面编辑器高度为 `clamp(300px, calc(100dvh - 300px), 420px)`，长文本内部滚动。
5. 底部操作区：拼接、拆分等转换按钮与复制按钮保留在双栏编辑区下方，横向排列并允许换行；这是用户明确要求保留的操作习惯，后续不得移回侧栏或顶部。
6. 状态区：位于操作区下方，原有实时提示使用 `role="status"` 与 `aria-live="polite"`。
7. 1100px 以下侧栏缩至 208px；768px 以下侧栏改为顶部控件区，隐藏辅助格式示例，编辑区上下排列。避免为了填满屏幕而添加无关卡片或假数据。

间距基于 4px：组内 8–12px，控件组 16–20px，区块间 24–28px。控件圆角 5–6px，面板 8px。只有浮层使用轻阴影。

## 交互与响应式

- 主操作：白字绿底，悬停深绿。次操作：白底细边框，悬停浅绿；不能只靠颜色表达业务差异，必须有明确文案。
- 焦点：清晰轮廓。编辑器用整块容器的 `:focus-within` 边框表达焦点。
- 预览：虚线警示边框，同时保留原有预览说明；不改变实际结果提交时机。
- 保留页面原有的隐藏、激活、只读、禁用等状态。本样板转换按钮由业务脚本添加 `.active` 后显示，不能用通用 `display` 覆盖。
- 768px 及以下：首页保留可横向滚动的常用工具栏，完整列表使用抽屉菜单；子页面左右边距 12px、面板内边距 14px，编辑器高度 210px，编辑区上下排列，回传按钮横向铺满，操作按钮换行。
- 不禁止浏览器缩放。控制长文本在编辑器内部滚动，避免撑宽页面。
- 图标必须配合文字或可访问名称。尊重 `prefers-reduced-motion`。

## 迁移约束与验收

只改展示层：保留原有 ID、data 属性、事件绑定依赖的 class、输入类型、option value、readonly、脚本引用和 DOM 关系。迁移前读脚本，特别注意 `closest()`、父子选择器、动态 `.active`、内联 `style.display` 等依赖。

不要改转换算法、网络请求、存储键、默认选项、路由、快捷键、自动复制或自动保存行为。装饰脚本可以移除，但必须明确区分装饰和业务；本次仅删除文本页粒子背景初始化及其依赖。

逐页验收：

- 比较改造前后业务脚本及交互节点，确认无意外变化。
- 运行代表性输入：空内容、普通内容、多行和长文本；检查该工具的核心转换及选项组合。
- 检查首页嵌入和直接打开两种入口，工具切换及输入恢复机制保持原样。
- 在宽屏、768px、390px 和 200% 缩放下检查排版，覆盖默认、输入后、结果、预览与菜单打开状态。实际未执行的检查应如实标记，不能声称已验证。

## 可交给其他 AI 的任务说明

> 请先读 AGENTS.md（不存在则 CLAUDE.md）、DESIGN-SYSTEM.md、styles/workbench.css，以及 index.html 和文本加工.html 两个样板。按既定工作台风格改造指定工具页，仅调整 HTML 展示结构和 CSS；保留全部业务脚本、交互节点标识、存储和路由行为。共享 token 直接引用，不引入框架、不影响未指定的页面。完成后说明变化与实际验证结果。

工具数量按 `#headerTabs.clientWidth` 计算，不能扣除其他行的品牌或搜索框宽度。所有宽度变化都应重建导航，维持收藏、最近使用、默认补位的既有顺序。

JSON 树形预览的折叠摘要应显示节点数量；对象优先补充自身 `name`，其次 `title`，数组优先补充首个对象元素中的这两个字段。每个非根节点提供删除入口，删除后必须同步更新输入 JSON、格式化和树形预览；路径需要保留原始对象键名和数组索引，不能通过替换字符生成可能碰撞的路径。

编码解码页采用同一套工作台结构：左侧为源内容输入，右侧依次放置功能分类、编码/解码操作和结果。功能按钮保持紧凑网格，结果区域不再与输入区等高，以避免空白；原有清空、复制、标签切换和全部转换逻辑必须保留。

文本对比页以紧凑标题、控制条、双栏输入和底部结果卡片组成工作区。双栏必须在视觉上标明 A、B 输入来源；逐行、集合、JSON 模式、筛选、搜索、结果导出及返回编辑的原有节点和行为不得改变。

UUID/随机数生成器以顶部类型标签和「左侧生成参数 + 右侧结果与说明」布局呈现。结果列表保持可滚动，单条复制按钮默认收起并在对应行悬停或聚焦时出现；UUID、随机字符串、随机数的生成、复制和默认生成行为不得改变。

二维码工具保留其生成卡片和历史卡片的辨识度，但页面容器、标题、标签、控件和色彩使用工作台 token。宽屏下生成参数置于左侧、二维码历史置于右侧；窄屏恢复纵向。前缀、搜索、生成、重置、清空、解码、拖拽、复制与弹窗节点及其存储行为不得改变。

Nacos 管理页采用紧凑标题、连接配置面板、标签切换和数据表工作区。连接状态、刷新和保存连接操作必须清晰可见；表格、加载/空/错误状态、分页、排序、服务实例弹窗，以及所有 API 请求和本地存储行为不得改变。
